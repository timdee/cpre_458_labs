package sensors;

import compute.CarPanelState;

public class StopSignSensor extends Sensor {
	public StopSignSensor(CarPanelState state) {
		super(state);
	}

	/**
	 * returns an array containing the distance of all stop signs within range
	 */
	public stop_sign_distance(){
		
	}
}
