package characters;

import java.awt.Color;

public class MainCar extends Car {
	public MainCar(int width, int height) {
		super();

		this.color = Color.BLUE;
		this.width = (int) (height * .1);
		this.height = this.height / 2;
		this.x_pos = ((int) ((this.width / 2) + width * .05));
		this.y_pos = this.height - this.height - (int) (height * .05);
		this.speed = 1;
	}
}
